name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3   
    - name: start randem b for
      run:  .\PmRandomBrute.ps1      
      shell: pwsh
